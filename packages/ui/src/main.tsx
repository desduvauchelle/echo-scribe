// placeholder — populated in step 0.5
import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>Echo Scribe UI placeholder</div>
  </React.StrictMode>
);
